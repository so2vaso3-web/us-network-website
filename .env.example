# Vercel KV/Redis (tự động từ Redis integration)
KV_REST_API_URL=
KV_REST_API_TOKEN=
REDIS_URL=

# Master Key cho encryption (tạo bằng: node -e "console.log(require('crypto').randomBytes(16).toString('hex'))")
MASTER_KEY=

# Base URL (tự động trên Vercel, chỉ cần set cho local dev)
# Production: https://zenith5g.com
# Local dev: http://localhost:3000
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Node Environment
NODE_ENV=production

